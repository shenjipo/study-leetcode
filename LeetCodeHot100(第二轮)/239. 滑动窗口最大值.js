/**
 * @param {number[]} nums
 * @param {number} k
 * @return {number[]}
 */
var maxSlidingWindow = function (nums, k) {
    if (nums.length <= k) {
        return [Math.max(...nums)]
    }
    this.stack = []
    let i = 0
    let ans = []
    while (i < nums.length) {
        if (!this.stack.length) {

        }
    }
};